## Bunch o' quick & dirty scripts

This "project" doesn't require any npm package.

Actually I don't think it has a package.json.

I just run `node some_script.js <args>` and pipe the result somewhere.

